"use strict";

const chai = require('chai');
const expect = chai.expect;
const should = chai.should();
const sinon = require('sinon');
const request = require('supertest')('http://localhost:3000');
const statusCodes = require('../../../constants/constants')["statusCodes"];

describe('Practice Using Mocks, Stubs, and Spies using sinon', () => {

    before(() => {
    
    });

    after(() => {
    
    });

    beforeEach(() => {
        
    });

    afterEach(() => {

    });

    describe('Practice Using Mocks in sinon', () => {

        it('', done => {
            
            done();  
        });
    });

    describe('Practice Using Stubs in sinon', () => {

        it('', done => {
            
            done();  
        });

    });

    describe('Practice Using Spies in sinon', () => {

        it('', done => {
            
            done();  
        });

    });

});